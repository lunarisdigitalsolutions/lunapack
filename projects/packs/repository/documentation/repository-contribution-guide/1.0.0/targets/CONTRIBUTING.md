# Contributing to {{ projectName }}

Search existing issues and pull requests before starting substantial work. Do
not include credentials, customer data, private URLs, or personal information
in issues, tests, examples, commits, or generated artifacts.

## Set up the repository

Run:

```text
{{ setupCommand }}
```

## Validate changes

Run:

```text
{{ validationCommand }}
```

Keep pull requests focused, explain the user-visible reason, and add tests for
changed behavior. Report vulnerabilities through
[Security]({{ securityUrl }}) and usage questions through
[Support]({{ supportUrl }}).
