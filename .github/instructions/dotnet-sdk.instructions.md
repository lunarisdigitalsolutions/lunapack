---
applyTo: 'global.json'
---

# .NET SDK Configuration

`global.json` pins the approved .NET SDK patch; update it only with a compatible
toolchain change and validate the solution build and test suite afterward.
